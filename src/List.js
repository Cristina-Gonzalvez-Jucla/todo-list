import React, { Component } from 'react';
import Ticket from './Ticket';

const Ticket = (props) => {
    return (
        <div>
            <h2>props.title</h2>
        </div>
    );
};

export default Ticket;